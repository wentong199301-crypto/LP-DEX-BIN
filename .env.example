# DEX Adapter Configuration
# Copy this file to .env and configure all required values

# =============================================================================
# Solana Configuration (REQUIRED)
# =============================================================================

# RPC endpoint (REQUIRED - no default)
SOLANA_RPC_URL=https://your-solana-rpc-endpoint.com

# Private key - choose ONE of the following methods:
# Option 1: Path to keypair JSON file (recommended)
SOLANA_KEYPAIR_PATH=/path/to/keypair.json

# Option 2: Base58 encoded private key (64 bytes)
# SOLANA_PRIVATE_KEY=your_solana_private_key_base58

# =============================================================================
# EVM Configuration (REQUIRED for ETH/BSC)
# =============================================================================

# RPC endpoints (REQUIRED - no default)
ETH_RPC_URL=https://your-ethereum-rpc-endpoint.com
BSC_RPC_URL=https://your-bsc-rpc-endpoint.com

# EVM private key (REQUIRED for executing swaps)
# Hex format with or without 0x prefix
EVM_PRIVATE_KEY=your_evm_private_key_hex

# 1inch API key (REQUIRED for ETH/BSC swaps)
# Get your API key from: https://portal.1inch.dev/
ONEINCH_API_KEY=your_1inch_api_key

# 1inch API URL (REQUIRED - no default)
ONEINCH_BASE_URL=https://api.1inch.dev/swap/v6.0

# =============================================================================
# Jupiter API Configuration (REQUIRED for Solana swaps)
# =============================================================================

# Note: public.jupiterapi.com has a 0.2% platform fee
JUPITER_QUOTE_URL=https://public.jupiterapi.com/quote
JUPITER_SWAP_URL=https://public.jupiterapi.com/swap
JUPITER_TOKEN_LIST_URL=https://tokens.jup.ag/tokens?tags=verified

# =============================================================================
# PancakeSwap API Configuration (REQUIRED for BSC LP)
# =============================================================================

PANCAKESWAP_BASE_URL=https://pancakeswap.finance/api/v0
